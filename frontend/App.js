import React, {Component} from "react";

class App extends Component {
  state = {
  }

  render(){

    return ( 
      <div id = "app">
      </div>
    );
  }
}

App.displayName = "MainApp";

export default App;
